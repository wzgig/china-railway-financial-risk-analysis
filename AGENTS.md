# 知世工作规则

## GitHub 同步规则

- 本文件夹 `知世/` 内的所有实质性改动，完成后都必须提交到 Git，并推送到已配置的 GitHub 远程仓库。
- 推送前必须运行 `git status --short` 检查改动范围，确认没有账号、Cookie、密钥、私人凭据等敏感信息。
- 每次提交前必须在相应项目的工作记录中写清楚做了什么；跨项目或根目录级改动同时记录到根目录 `工作记录.md`。
- 提交信息使用清晰的 conventional commit 风格，例如 `docs: add workspace github sync rule` 或 `feat(big-data): add network analysis pipeline`。
- 如果因为认证、网络、远程冲突等原因无法推送，必须在回复和工作记录中明确说明失败原因、已完成的本地提交，以及需要用户处理的事项。

## 当前目录边界

- `大数据作业/`：协鑫能科算电协同价值创造网络分析。
- `已完成作业_中国中铁财务风险管理分析/`：中国中铁财务风险管理分析归档。
- 不要随意移动、删除或重命名两个作业目录；若确需调整，先记录原因和影响范围。
