# 工作记录

## 2026-07-02

### 已完成

- 确认当前 Git 仓库根目录为 `D:/Qiuhua Wang/个人资料/闲鱼/知世`。
- 确认当前 GitHub 远程仓库为 `https://github.com/wzgig/china-railway-financial-risk-analysis.git`。
- 新增 `AGENTS.md`，写入后续 Codex 工作规则：`知世/` 内所有实质性改动完成后必须提交并推送到 GitHub，且必须记录清楚做了什么。
- 新增根目录 `README.md`，说明当前工作区包含 `大数据作业/` 与 `已完成作业_中国中铁财务风险管理分析/` 两个目录，并记录 GitHub 同步规则。
- 建立根目录 `工作记录.md`，用于记录跨项目或根目录级变更。
- 新增根目录 `.gitignore`，排除 `.env`、密钥文件、压缩包和视频文件等不适合直接进入普通 Git 仓库的内容。
- 提交前检查到 3 个超过 GitHub 普通仓库单文件限制的大型交付物：`交付.zip` 和两个完整展示视频 `mp4`。这些文件保留在本地，不纳入本次 Git 提交；如需同步，应改用 GitHub Releases 或 Git LFS。

### 推送要求

- 本次规则变更需要随当前工作区改动一起提交并推送到 GitHub。
- 后续每次完成文件改动后，都应先更新工作记录，再提交和推送。
