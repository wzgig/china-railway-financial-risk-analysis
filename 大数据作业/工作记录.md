# 工作记录

## 2026-07-02

### 已完成

- 阅读 `任务要求.md`，确认本次作业对象为协鑫能科（002015）算电协同价值创造网络分析。
- 明确交付物：代码脚本、CSV 数据集、5000-8000 字分析报告、3 分钟内演示视频。
- 检查中铁作业归档状态，当前根目录已保留 `大数据作业/` 和 `已完成作业_中国中铁财务风险管理分析/`，两个作业已经分开。
- 评估中铁作业经验的复用价值，确定可复用目录结构、来源记录、脚本流水线、网络分析和视频交付方法。
- 建立大数据作业项目目录：`configs/`、`data/`、`docs/`、`outputs/`、`paper/`、`scripts/`、`交付/` 等。
- 完成第一版作业计划、数据来源记录、需求拆解、网络建模方案和报告结构草案。
- 建立 `data/raw/source_manifest.csv`，记录第一批官方资料入口。
- 校验 `configs/network_schema.json`，确认 JSON 格式有效。
- 补充 `完整执行规划.md`，逐条映射代码、数据、报告、视频和交付要求。
- 补充 `新对话启动提示词.md`，便于新对话快速进入下一步脚本和数据构建工作。
- 编写并运行 `scripts/01_collect_public_sources.py`，读取 `data/raw/source_manifest.csv`，生成 `data/interim/source_check.csv`。本轮检查 7 条公开来源，7 条链接可访问。
- 编写并运行 `scripts/02_build_network_dataset.py`，生成 `data/processed/nodes.csv`、`data/processed/edges.csv` 和 `data/processed/data_dictionary.csv`。第一版网络包含 38 个节点、85 条边。
- 编写并运行 `scripts/03_analyze_network.py`，生成 `outputs/tables/network_metrics.csv`、`outputs/tables/key_paths.csv`、`outputs/tables/network_summary.csv`、`outputs/tables/community_summary.csv`，并同步生成 `data/processed/stage_summary.csv` 与 `outputs/tables/stage_summary.csv`。
- 编写并运行 `scripts/04_visualize_network.py`，生成 5 张报告用静态图和 `outputs/network/value_network_interactive.html` 交互网络图。
- 修复 `03_analyze_network.py` 中 NetworkX 建图时 `weight` 参数重复传入的问题。
- 修复 `04_visualize_network.py` 的中文字体设置，显式使用 `Microsoft YaHei` 生成网络标签，避免中文缺字方框。
- 抽检中心性指标：`AI赋能虚拟电厂平台`、`工商业用户侧负荷`、`能源调度模型`、`工业负荷模型`、`零碳园区能源平台` 等节点在 PageRank 或加权度上排名靠前，可支撑报告中的平台调度、负荷聚合、算法赋能和场景扩展解释。
- 抽检关键路径：7 条路径均可达，包括“清洁能源资源→AIDC/智算客户”“新型储能→辅助服务市场”“气象数据资源→调节收益分成机制”等。
- 更新 `README.md`，补充 01-04 脚本运行命令、已生成成果和数据边界说明。

### 下一步目标

- 将网络结果转化为报告中的成本管理洞察。
- 基于 `outputs/tables/network_metrics.csv` 和 `outputs/tables/key_paths.csv` 撰写 `paper/协鑫能科算电协同价值创造网络分析报告.md` 初稿。
- 补充 `docs/视频录制指南.md`，围绕交互网络图、中心性排名图和阶段演化图组织 3 分钟解说。
